import {ADD_PERSON} from '../constantStore'

const initState=['tom']

function person(prevState=initState,action){
    const {type} =action
    switch (type){
        case ADD_PERSON:

            break;

    }
        
}